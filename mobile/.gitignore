# Generated by Expo
.expo/
node_modules/

# This is a pnpm workspace — npm lockfiles in this subdir confuse EAS
# Build's package-manager detection and trigger a standalone npm install
# that misses workspace overrides (React 19.1.0 pin, hoisting). pnpm-lock
# lives at the repo root.
package-lock.json
yarn.lock

# Native projects — generated by `expo prebuild` (M1a Step 5 keeps the
# managed workflow). When recording lands in M3 we may switch to bare
# and commit ios/ to source control.
ios/
android/

# Bundles + caches
dist/
web-build/

# Local env overrides
.env
.env.local

# IDE
.vscode/
.idea/
*.log
.DS_Store

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli